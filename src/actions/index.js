export const user = values => ({
  type: 'SEND_FORM',
  values
})

export const viewForm = status => ({
  type: 'TOGGLE_FORM',
  status
})

export const prueba3 = values => ({
  type: 'CALORIES_REST',
  values
})